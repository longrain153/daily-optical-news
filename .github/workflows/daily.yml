name: 光通信每日简报

on:
  schedule:
    # 北京约 07:33 / 08:03 / 08:33 三次冗余触发（UTC 23:33 / 00:03 / 00:33）。
    # GitHub 定时常丢任务，多触发几次提高命中率；配合 SKIP_IF_DONE 去重，当天只发一次。
    - cron: "33 23 * * *"
    - cron: "3 0 * * *"
    - cron: "33 0 * * *"
  workflow_dispatch:

concurrency:
  group: daily-optical-news
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  brief:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: Install Chromium for screenshots
        run: python -m playwright install --with-deps chromium
      - name: Run daily brief
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          PAGES_BASE: https://longrain153.github.io/daily-optical-news
          SKIP_IF_DONE: ${{ github.event_name == 'schedule' && '1' || '' }}
        run: python main.py

      - name: Publish to GitHub Pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs
          if git diff --cached --quiet; then
            echo "no changes"
          else
            git commit -m "brief: publish $(date -u +%Y-%m-%d)"
            git push
          fi
